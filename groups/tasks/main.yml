---
## -------------------------------------- ##
## Creates or updates a group and its GID ##
## -------------------------------------- ##

- loop: "{{ user_groups }}"
  group:
    name: "{{ item.name | mandatory }}"
    gid: "{{ item.gid | mandatory }}"
