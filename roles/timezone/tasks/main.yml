---
## ---------------- ##
## Set the timezone ##
## ---------------- ##

- name: set the timezone
  community.general.timezone:
    name: "{{ timezone | mandatory }}"

- name: restart crond
  ansible.builtin.service:
    name: cron
    state: restarted
