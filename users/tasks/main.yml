---
## ---------------------------------- ##
## Creates or updates the given users ##
## ---------------------------------- ##

- name: ensure no targeted users are logged on
  reboot:
    reboot_timeout: 600

- name: create or update users
  loop: "{{ user_defs }}"
  loop_control:
    loop_var: user_def
  include: define_user.yml
