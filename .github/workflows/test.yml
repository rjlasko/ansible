name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  # ansible-lint:
  #   name: Lint
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Check out the codebase.
  #     uses: actions/checkout@v3
  #   # - name: Build the Docker image
  #   #   run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
  #
  #   - name: Ansible Lint
  #     # You may pin to the exact commit or the version.
  #     # uses: ansible-community/ansible-lint-action@ba54de22a6edeea3aa385cd8c5f20ec1f960a30b
  #     uses: ansible-community/ansible-lint-action@v6.0.2
  #     # uses: ansible-community/ansible-lint-action@v4.1.0.post0
  #     # with:
  #     #   # Arguments to be passed to the ansible-lint.
  #     #   args: ". --project-dir /home/runner/work/ansible/ansible/" # optional, default is .

  yaml-lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v3

      # - name: Check out the codebase.
      #   uses: actions/checkout@v2
      #   with:
      #     path: ansible_collections/geerlingguy/mac

      - name: Set up Python 3.
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install test dependencies.
        run: pip3 install yamllint ansible-core ansible-lint

      - name: Lint code.
        run: |
          yamllint .
          ansible-lint
