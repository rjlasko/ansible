---
pipx_bin_prefix: "{{ (pipx_venv + '/bin/') if pipx_installation == 'venv' else '' }}"

pipx_symlink: "{{ ansible_user_dir }}/bin/pipx"
pipx_venv: "{{ ansible_user_dir }}/bin/pipx_venv"

pipx_apps_path: "{{ ansible_user_dir }}/.local/bin"
pip_conf_filepath: ~/.config/pip/pip.conf
