---
## ------------- ##
## Installs jenv ##
## ------------- ##

- when: ansible_facts.system == 'Darwin'
  block:
    - name: install jenv (MacOS)
      community.general.homebrew:
        name: jenv
        state: present

    - name: get jenv root
      ansible.builtin.command: jenv root
      changed_when: false
      register: jenv_root

    - name: delete pre-existing jenv config (MacOS)
      when: java_reset_jenv
      ansible.builtin.file:
        path: "{{ jenv_root.stdout }}"
        state: absent

- when: ansible_facts.system == 'Linux'
  block:
    - name: delete pre-existing jenv installation (Linux)
      when: java_reset_jenv
      ansible.builtin.file:
        path: "{{ java_jenv_user_dir }}"
        state: absent

    - name: "install jenv package to {{ java_jenv_user_dir }} (Linux)"
      ansible.builtin.git:
        repo: https://github.com/jenv/jenv.git
        dest: "{{ java_jenv_user_dir }}"

    - name: "add jenv path to shell initialization script: {{ java_jenv_init_file }}"
      ansible.builtin.blockinfile:
        path: "{{ java_jenv_init_file }}"
        create: true
        insertbefore: BOF
        marker: "# --- {mark} jenv execpath ---"
        block: |
          if ! $(echo "$PATH" | tr ":" "\n" | grep -qx "{{ java_jenv_user_bin }}") ; then
              export PATH="{{ java_jenv_user_bin }}:$PATH"
          fi
        state: present
        mode: u=rw,go=r

    - name: first time init jenv
      environment:
        PATH: "{{ java_jenv_user_bin }}:{{ ansible_env.PATH }}"
      ansible.builtin.shell: 'eval "$(jenv init -)"'
      changed_when: false

- name: "add jenv init to shell initialization script: {{ java_jenv_init_file }}"
  ansible.builtin.blockinfile:
    path: "{{ java_jenv_init_file }}"
    create: true
    marker: "# --- {mark} jenv completions ---"
    block: 'eval "$(jenv init -)"'
    state: present
    mode: u=rw,go=r
